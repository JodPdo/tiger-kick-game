# CURRENT_PHASE.md — Tiger Kick

Single source of truth for "where are we now". **Update this whenever the phase/milestone/sprint moves.** Keep it tiny.

| Field | Value |
|---|---|
| Docs Version | v0.32 |
| Game Build | v0.4 — Phase 2 Core Loop CLOSED (Exit Gate PASS 2026-07-19): Kick + Tag + 7-step Tag Sequence + role swap + Pounce + Kick Stagger, all human-verified across 2 instances; Phase 3 First Playtest next |
| Current Phase | **Phase 3 — First Playtest** (started 2026-07-19) |
| Current Milestone | **M3 First Playtest** |
| Current Sprint | ▶️ ACTIVE — **Phase 3 board opened (2026-07-19).** Phase 2's full closure history now lives in the "Phase 2 — CLOSED" section below (this cell was a running Phase 2 log that grew too large — condensed there per the "keep it tiny" rule; full blow-by-blow detail for every Phase 2 card stays in `_backlog.json` and `03_Change_Log.md`, nothing lost). **First up:** `TK-P2-14` Countdown + `TK-P2-15` Match state machine (both carried over from Phase 2, both now gate `TK-P3-03`) — see "Next — Phase 3" below. ~~PRE-CLOSURE LOG (2026-07-14 → 2026-07-19)~~ **`TK-P2-02` Tag detection CLOSED (2026-07-14): human 2-instance test PASS 5/5** (in_range/left_range correct both directions, host-only-emit gate confirmed live [host is_server=true+fires, client is_server=false+silent], leave-range clears candidates, despawn-in-range no crash/no stale, floor never a candidate — via temp debug prints + DebugOverlay line, both reverted after). Observation for TK-P2-03: TagDetector's in_range/left_range can rapid-refire at the exact range boundary (Area3D edge jitter) — Tag Sequence must guard against re-trigger (see TK-P2-03 note). `TK-P2-24` CI headless tag-detect probe also CLOSED (review+qa PASS, CI-gated). **`TK-P2-03` Tag Sequence CLOSED (2026-07-14): human 2-instance test PASS 5/5** — step1→step7 both round-trips (~1.2s, within ≤1.5s budget); client log had zero `[TAG-SEQ]` lines (host-only confirmed); role swap matched both directions on both machines; re-trigger guard held (`[TAG] rejected: sequence_active` under mid-sequence spam); role-gated ability correct (Tiger→TAG, Outer→KICK); sequence-active lock cleared every round, no double-Tiger, no crash. TEMP DEBUG F7 hook (force-assigned first Tiger, stand-in for missing TK-P2-09) removed post-PASS. **`TK-P2-04` Role SM CLOSED (2026-07-14): code-review APPROVE + QA PASS-WITH-FOLLOWUP.** Investigation confirmed TK-P2-03/TK-P2-16 already covered role state + AbilityController distribution; genuinely missing pieces implemented: `characters/RoleRules.gd` valid-transition guard (Player.set_role() now fails closed on an invalid role), MovementComponent per-role speed profile (the card's "property" swap, wiring already-approved Game_Balance.md §2 values for the first time — Human walk4/sprint6, Tiger walk+sprint capped at 4.0), and a new `role_changed` signal hook left for TK-P2-05's camera swap (camera itself untouched, stays TK-P2-05). QA re-ran regression LOCALLY (Godot 4.7 headless, not just trusting the reported number): `--import` clean + GUT **238/238 (1378 asserts, 17 scripts)**, test_role_state_machine.gd 16/16, no S0/S1/orphans. Automation-only closure ACCEPTED for this card's scope — pure-logic wiring (role-value guard + speed-profile lookup + signal), ZERO new RPC/network/host-authority surface (role NOT on the synchronizer; assignment still GameManager.apply_role_switch() from TK-P2-03, already human-verified), so the network/desync risk class the TK-P2-02/03 human passes existed to catch is absent here. The one un-automatable thing (the FEEL of the new per-role speeds) is not observable until a Tiger exists live — filed **`TK-P2-27`** (qa, human speed-profile feel-test, depends_on TK-P2-09) to fold into the TK-P2-09/05 human pass. Followup `TK-P2-26` filed (tools-devops) for a pre-existing GUT test-suite-ordering hazard found + worked around test-locally. **`TK-P2-05` FP/TP camera swap CLOSED (2026-07-14): code-review APPROVE + QA PASS-WITH-FOLLOWUP.** New pure-static `CameraModeRules.is_first_person(role)` (tiger only, fails safe to third-person); `CameraComponent` subscribes to the `role_changed` hook TK-P2-04 left (signal-subscribe only, Player.gd untouched — resolves the TK-P2-04 nit) and `_apply_camera_mode()` swaps `SpringArm3D.spring_length` (FP length for Tiger / cached scene-authored TP length for Outer) + hides/shows the owner's own mesh, whole method gated on `is_multiplayer_authority()` so the FP swap stays LOCAL to the Tiger's own client (every other peer keeps seeing that Tiger third-person, model visible). ZERO new RPC/network/host-authority surface — role still arrives exclusively via GameManager.apply_role_switch() (TK-P2-03/04). QA re-ran regression LOCALLY (Godot 4.7 headless, not just trusting the reported number): `--import` clean + GUT **246/246 (1390 asserts, 18 scripts, 5.7s)**, test_camera_mode.gd 8/8 (CameraModeRules pure cases + owner AND non-owner authority-gated integration both asserted), no S0/S1/orphans (only ERROR/WARN lines are pre-existing intentional negative-path assertions). Automation-only closure ACCEPTED for Done — local view-only per-peer rendering, the live-network/desync risk class the TK-P2-02/03 human passes existed to catch is absent, and the authority gate + swap correctness are fully automation-covered. The one un-automatable thing (does the swap actually LOOK right across two real windows, Test Plan §2 DoD item 3) literally cannot be run right now — no live role-swap trigger exists (first-Tiger not merged, TK-P2-03 F7 debug hook removed post-close) — so that human FP/TP visual check was folded into **`TK-P2-27`** (now covers BOTH TK-P2-04's speed-profile feel AND TK-P2-05's camera visual check; +depends_on TK-P2-05) rather than opening a near-duplicate TK-P2-28, since both wait on the same one human N-window pass once a Tiger is observable. **`TK-P2-09` first-Tiger (host-authoritative random assign, replaces removed F7 hook) — CODE-COMPLETE, DUAL-REVIEWED (code-reviewer APPROVE + network-engineer network-safe), QA-AUTOMATION GREEN, BLOCKED ON REQUIRED HUMAN 2-INSTANCE PASS (NOT closed).** Host picks exactly one Tiger via a single seeded RNG (host-only, once) and broadcasts via new `assign_first_tiger` `@rpc(authority,call_local,reliable)`; wires pre-existing pure `TigerSelector.gd` (unchanged) into a real broadcast on `managers/GameManager.gd` (only product file touched). Real bug found+fixed in review: `child_entered_tree` fires before the spawned child's own `_ready()`, which would have skipped AbilityController/MovementComponent distribution for the new Tiger — fixed via `call_deferred()`. QA re-ran LOCALLY (Godot 4.7 headless, own run not just trusting the reported number): `--import` clean + GUT **246/246 (1390 asserts, 18 scripts, 5.67s)**, test_tiger_assignment.gd 9/9, no S0/S1/orphans; all 4 network probes re-run and PASS (net_smoke + spawn_probe + spawn_probe_together + tag_detect_probe). Automation-only closure REJECTED here (unlike TK-P2-04/05): this card adds a NEW live @rpc + host-authority selection surface — exactly the desync (S1) risk class the TK-P2-02/03 human 2-instance passes exist to catch, and the DoD explicitly requires it ("เปิด 2+ อินสแตนซ์แล้วทุกเครื่องเห็นเสือคนเดียวกันเสมอ, ไม่มีเสือซ้อน/เสือหาย") — both prior reviewers also said do NOT close on automation alone. Status set to `review-pending-human`; ready-to-run human 2-instance checklist appended to the card's notes (also unblocks TK-P2-27's deferred speed-profile feel + TK-P2-05's camera visual check — three birds, one pass). Next: a human runs the TK-P2-09 checklist → PASS → producer closes. — **match-flow entry + Jump PLAYABLE & human-verified (2026-07-13).** Closed this session: `TK-P2-12` Waiting Room, `TK-P2-13` Start Match (3-round opus review; killed a real S1 = Godot per-peer node-path-cache poisoning on scene transition, fixed via host-side **readiness barrier** + grace-timer force-disconnect), `TK-P2-10` Jump — all human N-window PASS (Groups A/B/C Start + B1/B2/B3 barrier + D roster + Jump a–e). `TK-P2-01` Kick, `TK-P2-16` scaffold also DONE. Automation: GUT 184/184, net_smoke + spawn_probe + spawn_probe_together CI-gated. **Now unblocked / next:** `TK-P2-14` Countdown (after Start), `TK-P2-11` Jump-Kick (after Jump), `TK-P2-17` Kick Stagger, `TK-P2-18` Pounce. **Open followups from TK-P2-13:** `TK-P2-20` late-join (network, S2), `TK-P2-21` B2 headless probe + wording (tools-devops), `TK-P2-22` nits bundle, `TK-P2-23` Change Log + TDD §9 barrier/poison doc (docs — do next for change-control), `TK-P2-19` CI kick-probe. **Agents:** architect + code-reviewer now opus (`a25043e`) — opus reviewer caught the TK-P2-13 S1 automation missed. **RESUME:** run the single human N-window checklist (Start happy-path N=3–8 + barrier B1/B2/B3 + WaitingRoom roster + Jump) → each card PASS → flip to done + close. Then next unblocked: `TK-P2-11` Jump-Kick (after Jump done), `TK-P2-14` Countdown (after Start done), `TK-P2-17` Kick Stagger, `TK-P2-18` Pounce. **Followup cards filed from TK-P2-13 review/qa:** `TK-P2-20` late-join-after-start (network, S2), `TK-P2-21` B2 headless probe + stale-wording fix (tools-devops), `TK-P2-22` nits bundle (grace-timer stop, disconnect_peer is_server guard, WaitingRoom literal-true + roster-broadcast guard, slot-capacity), `TK-P2-23` Change Log + TDD §9 barrier/poison doc (docs). Also open: `TK-P2-19` (CI kick-pipeline probe). **Agents:** architect + code-reviewer upgraded fable5→opus (`a25043e`) — opus reviewer caught the TK-P2-13 S1 that automation missed. **`TK-P2-09` CLOSED (2026-07-15): human 2-instance test PASS** — exactly one Tiger, every window agreed, no double/missing-Tiger. Flipped to `done`; unblocks `TK-P2-27` (all deps now done) and gives `TK-P2-07` a real call site. **Design ruling recorded for `TK-P2-06` (Safe Circle, Change Log [0.42]):** Tiger observed walking out of the circle during the TK-P2-09 pass is NOT a bug — it's TK-P2-06 scope. Ruling: Tiger confined inside radius 5.0m (invisible wall, value locked at [0.34]), Outer players move freely in/out, host-authoritative enforcement (host clamps/rejects position, no client-side escape). **`TK-P2-06` Safe Circle — CODE-COMPLETE, DUAL-REVIEWED, QA-AUTOMATION GREEN, BLOCKED ON REQUIRED HUMAN 2-INSTANCE PASS (NOT closed, 2026-07-16).** Tiger confined to 5.0m radius (owner-side clamp in `MovementComponent.gd` + host-side `correct_tiger_position` backstop in `GameManager.gd`), Outer players unrestricted. code-reviewer APPROVE-WITH-NITS; network-engineer first round NOT network-safe (host boundary check had zero tolerance → reliable RPC broadcast spam ~15-30/sec against an honest wall-pinned Tiger) → gameplay-engineer fixed (0.05m tolerance host-side only, RPC retargeted to owning peer via `rpc_id`, misleading comment corrected, 4 new tests). QA re-ran independently: GUT **276/276 (1428 asserts, 19 scripts)**, all 4 net probes PASS, DoD-vs-design-ruling checked test-by-test. Automation-only closure REJECTED (same S1-desync risk class as TK-P2-02/03/09/12/13 — host-authoritative position + a live correction RPC). Status `review-pending-human`; 5-case human 2-instance checklist written into the card's `notes` in `_backlog.json` (Tiger cannot exit; confinement agrees across windows; correction path is a total no-op / zero RPC spam against a wall-pinned honest Tiger — the exact bug just fixed; Outer walks freely in/out; role-swap transfers confinement correctly). **Non-blocking follow-up filed:** `TK-P2-28` (architect) — whether Phase 2 accepts owner-authoritative-position defense-in-depth for the Tiger boundary, or true host-authoritative Tiger position is warranted; does not block this card's human pass. **`TK-P2-06` CLOSED (2026-07-16): human 2-instance test PASS** — Tiger cannot walk through the 5.0m wall, Outer players move in/out freely, pushing the wall produces no jitter/rubber-band (live confirmation the tolerance fix held). **2 non-blocking follow-ups filed:** `TK-P2-29` (gameplay-engineer — wire Tag Sequence step 6's exit teleport to the real Safe Circle boundary instead of its old placeholder) and `TK-P2-30` (network-engineer — review whether the Start-Match 8s readiness grace-timer is tolerant enough for a slow-but-legit client, related to `TK-P2-20`). `TK-P2-28` (architect ruling on true host-authoritative Tiger containment) still open. **`TK-P2-28` CLOSED (2026-07-16): architect ruled APPROVE** — owner-authoritative Tiger position + host best-effort correction is the Phase 2-correct level (design ruling [0.42] pt.3 is satisfied by the honest-client path; game-deciding state — role/tag/kick — stays fully host-authoritative regardless of a spoofed position); true host-authoritative Tiger position deferred to Phase 3+ anti-cheat hardening (not a Phase 2 must-fix, nothing scheduled). Recorded Change Log [0.45] + a forward-pointer "Known Limitations — Phase 2→3" section added to Game_Balance.md §6 so the deferral isn't lost. **`TK-P2-07` RoundManager — CODE-COMPLETE, DUAL-REVIEWED (code-reviewer APPROVE-WITH-NITS), QA-AUTOMATION GREEN, BLOCKED ON REQUIRED HUMAN 2-INSTANCE PASS (NOT closed, 2026-07-16).** New scene-local host-authoritative `managers/RoundManager.gd` (sibling of GameManager under world/TestArena.tscn, placed BEFORE GameManager in child order — load-bearing: its `role_changed` watch must be wired before GameManager's synchronous standalone-boot path can fire round-1's first Tiger) + pure `managers/RoundRules.gd` (timeout/remaining-time math, same pure/shell split as TagSequenceRules/SafeCircleRules). Timer-only round-end per GDD's committed baseline ("...จนหมดเวลา") — GDD §9 leaves alternate end-conditions (kick count etc.) an explicit open question deferred to `TK-P3-04`, deliberately NOT implemented here. Re-picks the new Tiger via TigerSelector.pick_first_tiger() reusing its pre-existing `avoid` param so a timed-out round doesn't hand the reign right back to the same Tiger; this was an unescalated engineering judgment call code-reviewer flagged — **designer ruled APPROVE as a Phase 2 placeholder (Change Log [0.46])**, revisitable at TK-P3-04 once real playtest data exists. New live `@rpc("authority","call_local","reliable") start_new_round` broadcast — same S1-desync risk class as TK-P2-02/03/06/09, so code-reviewer + qa-engineer both require a human 2-instance pass before Done (GUT/probes can't exercise a live role-swap). QA independently re-ran regression LOCALLY: GUT **289/289 (1640 asserts, 20 scripts)**, all 4 net probes PASS, no S0/S1/orphans; a 5-case human 2-instance checklist is written into the card's `notes` in `_backlog.json` (shrink round_duration_sec for the test session; round-1 timer arms; timeout → every window agrees on the same single new Tiger; Safe Circle confinement transfers from ex-Tiger to new Tiger; anti-repeat noted as an N=2 test-session limitation, not a failure condition). Non-blocking nit open: round-1's timer-start ordering depends on scene sibling order with no code-level safeguard against a future TestArena.tscn reorder (recommended fix: an existing-Tiger scan in `_ready()` to make it order-independent — not yet actioned). **`TK-P2-07` CLOSED (2026-07-16): human 2-instance test PASS** — CASE 1-4 all green (round-1 timer arms via the sibling-order path, timeout swap agrees on the same single new Tiger across windows, no double/missing Tiger across repeated cycles, Safe Circle confinement transfers ex-Tiger-to-new-Tiger correctly); BONUS also re-confirmed TK-P2-27 item (b) FP/TP camera swap-by-role live across 2 windows during the timeout swap (TK-P2-27 stays open, still needs its own (a) speed-profile-feel pass). Temp test override (`round_duration_sec` 240→12 for the test session, scene-only) reverted, confirmed 240.0 restored. **1 new non-blocking followup filed:** `TK-P2-31` (gameplay-engineer) — undefined/unverified behavior when a round times out with no other candidate for TigerSelector to swap to (N=1 edge case), doesn't block Phase 2 loop-proving. **`TK-P2-08` CLOSED (2026-07-16): already-satisfied, no code change.** Card predates the TK-P2-16 Ability System redesign and was silently orphaned by it — AbilityController's 3-RPC surface (`rpc_request_activate`/`host_validate`/`rpc_confirm`) already delivers exactly "client sends command via RPC, host confirms result" for both Kick (TK-P2-01) and Tag (TK-P2-03), both already human 2-instance PASS'd; TK-P2-02's host-only-emit gate keeps the underlying detection feed trustworthy too. network-engineer independently audited (read AbilityController/Ability/KickAbility/TagAbility/AbilityCatalog/TagDetectorComponent + design doc §4/4a/4b in full): actor identity + target are always host-derived, no client-spoofable field exists, no remaining client-authoritative gap. Closed as already-satisfied, full traceability recorded in the card's own notes. **`TK-P2-18` Pounce (TigerAbility, host-authoritative burst movement) — IN PROGRESS (gameplay-engineer, prioritized ahead of `TK-P2-17`/`TK-P2-27` per producer call: the "Tiger is slow, hunt-not-chase" design rule can't be feel-tested without it).** **`TK-P2-18` Pounce — CODE-COMPLETE, DUAL-REVIEWED (code-reviewer APPROVE-WITH-NITS, all 8 scrutinized design points held: owner-side execution, override-not-blend movement, rejection correctly cancels in-flight burst without touching position, Safe Circle still clamps post-burst), QA-AUTOMATION GREEN (GUT 309/309, 4/4 net probes), BLOCKED ON REQUIRED HUMAN 2-INSTANCE PASS (NOT closed, 2026-07-16/17).** New `characters/abilities/PounceAbility.gd extends TigerAbility` (~8 m/s / ~0.35s burst, 2.0s cooldown, all @export placeholders pending designer lock) + `PounceRules.gd` (pure cooldown) + a new burst-override seam on `MovementComponent.gd` (`start_pounce_burst`/`cancel_pounce_burst`/`resolve_horizontal_velocity`, mirrors `step_jump()`'s override shape on the horizontal axis) + catalog registration (`ROLE_TIGER` now `[TagAbility.gd, PounceAbility.gd]`) + new InputMap action `pounce` (RMB, no conflict). Movement stays owner-side/owner-authoritative per the TK-P2-28 ruling; `host_validate()` only gates cooldown legality, no target selection (Pounce doesn't catch anyone — TagAbility remains the sole catch mechanic, scope boundary explicitly held). 6-case human checklist written into the card's notes (burst-in-held-direction, camera-forward fallback, cross-window replication, Safe Circle interaction, cooldown enforcement, best-effort mid-sequence rejection). **`TK-P2-17` Kick Stagger — CODE-COMPLETE, DUAL-REVIEWED (code-reviewer APPROVE-WITH-NITS), QA-AUTOMATION GREEN (GUT 336/336, 4/4 net probes), BLOCKED ON REQUIRED HUMAN 2-INSTANCE PASS (NOT closed, 2026-07-16/17).** New `characters/components/StaggerRules.gd` (pure direction/decay math) + a stagger seam on `MovementComponent.gd` that ADDS decaying knockback on top of WASD/Pounce (never overrides — the hard "stagger not stun" card requirement is satisfied structurally, verified in code) + `KickAbility.gd`'s `on_confirmed()` now detects "am I the confirmed target" off the EXISTING `rpc_confirm` broadcast (no new RPC) and applies the stagger to itself locally. Reviewer nit (non-blocking, feeds a possible TK-P3-04 tuning follow-up): `stagger_knockback_speed_mps` (4.0, placeholder) equals the Tiger's own walk speed, so a fast-reflex Tiger holding a key toward the kicker can largely cancel the push — human pass should form a subjective read on whether the escape window still feels meaningful. 4-case human checklist written into the card's notes. **`TK-P2-32` (bug, Kick targeting) CLOSED (2026-07-17): fixed + automation-only close, no human pass needed.** Found during TK-P2-17's own review: `KickAbility.host_validate()` was never filtered to `role == tiger` (the TK-P2-01 card note anticipated this exact filter but it was never applied even after TK-P2-04/09 landed) — at N>=3 a Kick could confirm against the wrong (non-Tiger) player; invisible at every prior N=2 human pass. One-line filter fix + two new N=3 GUT regression cases (real `Player.tscn` scene tree, proven load-bearing by revert-and-confirm-fail). code-reviewer + qa-engineer both independently concurred no human pass is needed (zero new RPC/host-authority surface, only which candidate gets selected changed, bug inherently invisible at N=2 so a repeat live pass adds no signal). **`TK-P2-08` CLOSED (2026-07-16): already-satisfied, no code change** — see prior entry. **Next:** run the TK-P2-17 + TK-P2-18 human 2-instance checklists (can do both in one session, same Tiger+Outer setup) → PASS both → producer closes → then `TK-P2-27` feel-test pass (now able to cover the full Tiger kit: Pounce + Kick + Stagger + correct targeting); also still open: `TK-P2-29` Tag Sequence step-6 Safe Circle wiring, `TK-P2-31` RoundManager N=1 edge case. **`TK-P2-30` (bug, S2, readiness-barrier grace-timer force-disconnecting a legit unfocused client) fixed (grace now EXTENDS up to 3x for a still-ENet-connected peer instead of hard-kicking, ~32s bound, `networking/ArenaBarrierRules.gd`), dual-reviewed, QA PASS, CLOSED (2026-07-19): human 2-instance PASS — alt-tabbed client during Start Match no longer dropped. **`TK-BUG-P2-01`** (2nd occurrence of the wiring-dead-code bug class after TK-P2-32 — `KickAbility.on_confirmed()`'s stagger gate checked the KICKER's own node instead of resolving the TARGET, permanently-false condition, `start_stagger()` was structurally unreachable at any N; found live by human 2-instance test on TK-P2-17, 12 kicks/zero stagger) fixed (resolve target node + gate on `is_multiplayer_authority()`), dual-reviewed, QA PASS (independently reproduced RED-then-GREEN), CLOSED. **`TK-P2-17` CLOSED (2026-07-19): human 2-instance retest PASS** (CASE 1 + CASE 2 only, re-authorized post-fix) — stagger now visible + WASD/mouse-look stayed responsive throughout, replicated correctly cross-window. **`TK-P2-18` CLOSED (2026-07-19): human 2-instance test PASS** — Pounce replicates cross-window, cannot punch through the 5.0m Safe Circle wall. **`TK-P2-33`** filed (qa-engineer) — add regression coverage that positively asserts an ability's side-effect fired (not just no-crash), since this wiring-dead-code class has now recurred twice; should land before Phase 2 Exit Gate. **Next:** `TK-P2-27` feel-test pass (Tiger kit now complete: Pounce+Kick+Stagger+targeting all live-verified), `TK-P2-29`, `TK-P2-31`, `TK-P2-33`; Phase 2 Exit Gate getting close. **Producer ruling (2026-07-19, Change Log [0.49]):** no 2nd tester currently available — `TK-P2-27` re-scoped to a SOLO feel pass (speed-profile feel, Pounce feel, kick-flee rhythm); the "fun with real people" verdict formally moves to `TK-P3-03` (Phase 3, already designer-owned) rather than staying implicit in TK-P2-27. New card `TK-P3-07` filed (tools-devops) — a test bot extending the tests/net headless harness (approach Tiger → kick → flee) to make solo and future N≥3 testing possible without waiting on tester availability. Phase 2 Exit Gate criteria unchanged: core-loop-playable + no open S0/S1, already proven; the fun/balance verdict was always Phase 3's job. **Producer ruling (2026-07-19, Change Log [0.50]):** deferred `TK-P2-11` (Jump Kick), `TK-P2-14` (Countdown), `TK-P2-15` (Match state machine + MatchEnd), `TK-P2-20` (late-join), `TK-P2-33` (regression pattern) to Phase 3 as carry-overs — none were ever started or are S0/S1, so none block the Exit Gate. `TK-P2-14`+`TK-P2-15` now formally gate `TK-P3-03` (must land at the START of Phase 3, before playtesting — a session needs a synced countdown and a way to actually END). `TK-P2-29`/`TK-P2-31` stay open in Phase 2 as non-blocking carry-over debt (reaffirmed, not relabeled). **PHASE 2 REMAINS OPEN — closure conditions:** (1) `TK-P2-27`'s solo feel pass run + recorded PASS (not yet run), (2) P2-29/31 ruling recorded (✅ done, [0.50]), (3) full regression suite re-confirmed green at close time. Producer flips this section to CLOSED only after (1) reports PASS. |
| Exit Gate goal | Phase 2 goal (MET, see "Phase 2 — CLOSED" below): Kick + Tag + 7-step Tag Sequence + role swap on the ability scaffold; core loop playable. **Phase 3 goal (current):** Countdown + MatchEnd shipped, a real N≥2 playtest session run (`TK-P3-03`) and its verdict on whether the core loop is fun recorded, balance tuned against GDD Open Questions (`TK-P3-04`). |

## Phase 0 — CLOSED ✅ (Exit Gate PASS, PR #1 merged)
- clone→opens · 2 instances connect · `[NET] peer joined` · GUT green, no S0/S1. Cards `TK-P0-01`…`TK-P0-06`.

## Phase X — CLOSED ✅ (Exit Gate PASS, PR #2 merged `fd82306`)
- [x] Debug overlay (F3) + Perf overlay (F4) toggles, FPS/ping/state (manual GUI confirmed)
- [x] Settings change→save→reload persists across restart (headless round-trip + manual GUI confirmed)
- [x] ≥1 unit-test suite passing (GUT 88/88, 1135 asserts), Regression + NET SMOKE PASS, no S0/S1
- Cards `TK-PX-01`…`TK-PX-07`. Autoloads: GameLog, ConfigManager, NetworkManager, ErrorHandler, DebugOverlay, PerfOverlay.

## Phase 1 — CLOSED ✅ (Exit Gate PASS — human 2-window test + CI green)
- 6 build cards `TK-P1-01`…`TK-P1-06` + audit reopen fixes `TK-BUG-P1-01` (spawn authority/position via spawn_function + `_enter_tree`), `TK-BUG-P1-02` (host-quit → MainMenu + ESC-ESC Leave), `TK-P1-07` (CI gates on net smoke + spawn probe).
- [x] each peer spawns at its own slot (no stack/origin) + is authority of its own player
- [x] host quits → client returns to MainMenu; ESC-ESC Leave works (single ESC does NOT disconnect)
- [x] movement/camera sync across 2 windows — **human 2-window test PASSED**
- [x] Regression: GUT 119/119 (1206 asserts) + net_smoke + spawn probe (now CI-gated), no open S0/S1
- [x] Merge `develop` → `main` — **DONE**: Phase 1 close (`400e7f9` Exit Gate PASS) landed in `main` via **PR #4** (`386cf22`, 2026-07-05), which absorbed the pending PR #3. Phase 1 fully on `main`.
- Audit note [0.28]: S1-A "WASD dead (device:16)" was a FALSE ALARM (human-verified); the 116/116-green-but-broken lesson → per-card pure-function tests missed glue bugs; fixed by adding the 2-instance spawn probe to CI.

## Phase 2 — CLOSED ✅ (Exit Gate PASS, 2026-07-19 — Change Log [0.51])
- **Exit Gate goal met:** Kick + Tag + 7-step Tag Sequence + role swap on the ability scaffold; core loop playable. Every mechanic has both an automation-green regression AND a human 2-instance (or solo, for `TK-P2-27`) PASS on record — see each card's `note`/`human_pass_result` in `_backlog.json` for full detail.
- **Delivered:** Ability System scaffold (`TK-P2-16`), Kick (`TK-P2-01`) + Kick Stagger (`TK-P2-17`), Tag detection (`TK-P2-02`) + 7-step Tag Sequence (`TK-P2-03`), Role state machine (`TK-P2-04`), FP/TP camera swap (`TK-P2-05`), Safe Circle (`TK-P2-06`), RoundManager round loop (`TK-P2-07`), client→host RPC pattern already-satisfied (`TK-P2-08`), first-Tiger assignment (`TK-P2-09`), Jump (`TK-P2-10`), Waiting Room (`TK-P2-12`), Start Match + readiness barrier (`TK-P2-13`), Pounce (`TK-P2-18`), plus bugs found+fixed along the way (`TK-P2-32` kick-targeting, `TK-BUG-P2-01` stagger dead-code, `TK-P2-30` [S2] readiness-barrier grace-timer).
- **Final regression (re-run 2026-07-19, Godot 4.7.stable win64 headless, own run):** `--headless --import` clean, zero SCRIPT ERROR/Parse Error. **GUT 351/351 passing (1723 asserts, 23 scripts, 7.8s)**, no S0/S1/orphans. All 5 net probes PASS: `run_net_smoke.sh`, `run_spawn_probe.sh`, `run_spawn_probe_together.sh`, `run_tag_detect_probe.sh` (benign engine-teardown noise only), `run_spawn_probe_slowjoin.sh`.
- **`TK-P2-27` solo feel pass PASS (2026-07-19):** speed-profile feel (Outer 4.0/x1.5 vs Tiger 4.0/4.0-capped), Pounce feel, kick-flee rhythm — nothing broken/off that needs a fix before other people play. Re-scoped from a blocked 2-instance pass to solo per producer ruling [0.49] (no 2nd tester available); the "fun with real people" verdict formally moved to `TK-P3-03`.
- **Deferred to Phase 3 as carry-overs** (producer ruling, Change Log [0.50] — none were started, none are S0/S1, none blocked this gate): `TK-P2-11` Jump Kick, `TK-P2-14` Countdown, `TK-P2-15` Match state machine + MatchEnd, `TK-P2-20` late-join handling, `TK-P2-33` positive-side-effect regression pattern. **`TK-P2-14` + `TK-P2-15` now gate `TK-P3-03`** — must land before the first real-people playtest.
- **Kept open as non-blocking Phase 2 debt** (reaffirmed via ruling, not gate-relevant): `TK-P2-29` (Tag Sequence step 6 / Safe Circle wiring), `TK-P2-31` (RoundManager N=1 edge case) — see "Open cross-phase items" below.
- Full blow-by-blow history of every Phase 2 card (reviews, QA verdicts, human-pass checklists, bugs found) preserved in the Current Sprint cell above (pre-closure log) and in `_backlog.json` / `03_Change_Log.md` [0.30]–[0.51].

## Open cross-phase items (not blocking)
- `TK-P2-09` TigerSelector helper landed early (partial); full card OPEN pending `TK-P2-04` (Phase 2). Architect ack on `managers/` placement deferred to Phase 2.
- Phase-1 wiring picks up deferred infra: define InputMap actions (move/kick/tag) — unblocks Settings→Controls rebind (+ reconcile rebind string format "W"→"w"); audio Music/SFX bus layout still pending.
- Tech-debt: NetworkManager peer-teardown guard / MainMenu nits (task); PDF docs regen to 4.7.
- Phase 2 non-blocking carry-over debt (reaffirmed by ruling, Change Log [0.50], not gate-relevant): `TK-P2-29` (gameplay-engineer — wire Tag Sequence step 6's exit teleport to the real Safe Circle boundary, currently a stale placeholder), `TK-P2-31` (gameplay-engineer — RoundManager N=1 edge case, undefined behavior when a round times out with no other candidate Tiger).

## Next — Phase 3 (First Playtest), the "is it actually fun" milestone
**Gate before `TK-P3-03` (First Playtest session):** `TK-P2-14` Countdown + `TK-P2-15` Match state machine (incl. **MatchEnd**, currently undefined anywhere in the codebase) must land first — carried over from Phase 2 (never started there, not S0/S1, didn't block that Exit Gate), but a real-people playtest session needs a synced countdown into the match and a way for it to actually end, or the session reads as broken/unfinished. `TK-P3-03.depends_on` already includes both.
- **Board order:** `TK-P2-14` Countdown → `TK-P2-15` Match state machine → `TK-P3-01` (export/tunable vars) → `TK-P3-02` (test build) → `TK-P3-03` (playtest session, now gated as above) → `TK-P3-04` (tune from playtest data, GDD Open Questions). Parallel/independent: `TK-P3-05` Tiger body-language (Crouch→Lean→Peek), `TK-P3-06` Charged Kick/Knockdown (design-pending, needs designer to lock tunables), `TK-P3-07` test bot (tools-devops, unblocks solo/N-player testing going forward), `TK-P2-11` Jump Kick, `TK-P2-20` late-join handling, `TK-P2-33` positive-side-effect regression pattern (do before `TK-P3-06` touches similar glue code).
- Ability catalog (Phase 3-5): Tiger{Crouch, Lean, Sprint, Pounce, Peek} · Human{Kick, Hide, Emote}.
