# CURRENT_PHASE.md — Tiger Kick

Single source of truth for "where are we now". **Update this whenever the phase/milestone/sprint moves.** Keep it tiny.

| Field | Value |
|---|---|
| Docs Version | v0.31 |
| Game Build | v0.3 — movement done (spawn/authority/camera/sync, 2-window verified); core loop next |
| Current Phase | **Phase 2 — Core Loop** (started; Ability System design APPROVED) |
| Current Milestone | **M2 Core Gameplay** |
| Current Sprint | ▶️ ACTIVE — **`TK-P2-02` Tag detection CLOSED (2026-07-14): human 2-instance test PASS 5/5** (in_range/left_range correct both directions, host-only-emit gate confirmed live [host is_server=true+fires, client is_server=false+silent], leave-range clears candidates, despawn-in-range no crash/no stale, floor never a candidate — via temp debug prints + DebugOverlay line, both reverted after). Observation for TK-P2-03: TagDetector's in_range/left_range can rapid-refire at the exact range boundary (Area3D edge jitter) — Tag Sequence must guard against re-trigger (see TK-P2-03 note). `TK-P2-24` CI headless tag-detect probe also CLOSED (review+qa PASS, CI-gated). **`TK-P2-03` Tag Sequence CLOSED (2026-07-14): human 2-instance test PASS 5/5** — step1→step7 both round-trips (~1.2s, within ≤1.5s budget); client log had zero `[TAG-SEQ]` lines (host-only confirmed); role swap matched both directions on both machines; re-trigger guard held (`[TAG] rejected: sequence_active` under mid-sequence spam); role-gated ability correct (Tiger→TAG, Outer→KICK); sequence-active lock cleared every round, no double-Tiger, no crash. TEMP DEBUG F7 hook (force-assigned first Tiger, stand-in for missing TK-P2-09) removed post-PASS. **`TK-P2-04` Role SM CLOSED (2026-07-14): code-review APPROVE + QA PASS-WITH-FOLLOWUP.** Investigation confirmed TK-P2-03/TK-P2-16 already covered role state + AbilityController distribution; genuinely missing pieces implemented: `characters/RoleRules.gd` valid-transition guard (Player.set_role() now fails closed on an invalid role), MovementComponent per-role speed profile (the card's "property" swap, wiring already-approved Game_Balance.md §2 values for the first time — Human walk4/sprint6, Tiger walk+sprint capped at 4.0), and a new `role_changed` signal hook left for TK-P2-05's camera swap (camera itself untouched, stays TK-P2-05). QA re-ran regression LOCALLY (Godot 4.7 headless, not just trusting the reported number): `--import` clean + GUT **238/238 (1378 asserts, 17 scripts)**, test_role_state_machine.gd 16/16, no S0/S1/orphans. Automation-only closure ACCEPTED for this card's scope — pure-logic wiring (role-value guard + speed-profile lookup + signal), ZERO new RPC/network/host-authority surface (role NOT on the synchronizer; assignment still GameManager.apply_role_switch() from TK-P2-03, already human-verified), so the network/desync risk class the TK-P2-02/03 human passes existed to catch is absent here. The one un-automatable thing (the FEEL of the new per-role speeds) is not observable until a Tiger exists live — filed **`TK-P2-27`** (qa, human speed-profile feel-test, depends_on TK-P2-09) to fold into the TK-P2-09/05 human pass. Followup `TK-P2-26` filed (tools-devops) for a pre-existing GUT test-suite-ordering hazard found + worked around test-locally. **`TK-P2-05` FP/TP camera swap CLOSED (2026-07-14): code-review APPROVE + QA PASS-WITH-FOLLOWUP.** New pure-static `CameraModeRules.is_first_person(role)` (tiger only, fails safe to third-person); `CameraComponent` subscribes to the `role_changed` hook TK-P2-04 left (signal-subscribe only, Player.gd untouched — resolves the TK-P2-04 nit) and `_apply_camera_mode()` swaps `SpringArm3D.spring_length` (FP length for Tiger / cached scene-authored TP length for Outer) + hides/shows the owner's own mesh, whole method gated on `is_multiplayer_authority()` so the FP swap stays LOCAL to the Tiger's own client (every other peer keeps seeing that Tiger third-person, model visible). ZERO new RPC/network/host-authority surface — role still arrives exclusively via GameManager.apply_role_switch() (TK-P2-03/04). QA re-ran regression LOCALLY (Godot 4.7 headless, not just trusting the reported number): `--import` clean + GUT **246/246 (1390 asserts, 18 scripts, 5.7s)**, test_camera_mode.gd 8/8 (CameraModeRules pure cases + owner AND non-owner authority-gated integration both asserted), no S0/S1/orphans (only ERROR/WARN lines are pre-existing intentional negative-path assertions). Automation-only closure ACCEPTED for Done — local view-only per-peer rendering, the live-network/desync risk class the TK-P2-02/03 human passes existed to catch is absent, and the authority gate + swap correctness are fully automation-covered. The one un-automatable thing (does the swap actually LOOK right across two real windows, Test Plan §2 DoD item 3) literally cannot be run right now — no live role-swap trigger exists (first-Tiger not merged, TK-P2-03 F7 debug hook removed post-close) — so that human FP/TP visual check was folded into **`TK-P2-27`** (now covers BOTH TK-P2-04's speed-profile feel AND TK-P2-05's camera visual check; +depends_on TK-P2-05) rather than opening a near-duplicate TK-P2-28, since both wait on the same one human N-window pass once a Tiger is observable. **`TK-P2-09` first-Tiger (host-authoritative random assign, replaces removed F7 hook) — CODE-COMPLETE, DUAL-REVIEWED (code-reviewer APPROVE + network-engineer network-safe), QA-AUTOMATION GREEN, BLOCKED ON REQUIRED HUMAN 2-INSTANCE PASS (NOT closed).** Host picks exactly one Tiger via a single seeded RNG (host-only, once) and broadcasts via new `assign_first_tiger` `@rpc(authority,call_local,reliable)`; wires pre-existing pure `TigerSelector.gd` (unchanged) into a real broadcast on `managers/GameManager.gd` (only product file touched). Real bug found+fixed in review: `child_entered_tree` fires before the spawned child's own `_ready()`, which would have skipped AbilityController/MovementComponent distribution for the new Tiger — fixed via `call_deferred()`. QA re-ran LOCALLY (Godot 4.7 headless, own run not just trusting the reported number): `--import` clean + GUT **246/246 (1390 asserts, 18 scripts, 5.67s)**, test_tiger_assignment.gd 9/9, no S0/S1/orphans; all 4 network probes re-run and PASS (net_smoke + spawn_probe + spawn_probe_together + tag_detect_probe). Automation-only closure REJECTED here (unlike TK-P2-04/05): this card adds a NEW live @rpc + host-authority selection surface — exactly the desync (S1) risk class the TK-P2-02/03 human 2-instance passes exist to catch, and the DoD explicitly requires it ("เปิด 2+ อินสแตนซ์แล้วทุกเครื่องเห็นเสือคนเดียวกันเสมอ, ไม่มีเสือซ้อน/เสือหาย") — both prior reviewers also said do NOT close on automation alone. Status set to `review-pending-human`; ready-to-run human 2-instance checklist appended to the card's notes (also unblocks TK-P2-27's deferred speed-profile feel + TK-P2-05's camera visual check — three birds, one pass). Next: a human runs the TK-P2-09 checklist → PASS → producer closes. — **match-flow entry + Jump PLAYABLE & human-verified (2026-07-13).** Closed this session: `TK-P2-12` Waiting Room, `TK-P2-13` Start Match (3-round opus review; killed a real S1 = Godot per-peer node-path-cache poisoning on scene transition, fixed via host-side **readiness barrier** + grace-timer force-disconnect), `TK-P2-10` Jump — all human N-window PASS (Groups A/B/C Start + B1/B2/B3 barrier + D roster + Jump a–e). `TK-P2-01` Kick, `TK-P2-16` scaffold also DONE. Automation: GUT 184/184, net_smoke + spawn_probe + spawn_probe_together CI-gated. **Now unblocked / next:** `TK-P2-14` Countdown (after Start), `TK-P2-11` Jump-Kick (after Jump), `TK-P2-17` Kick Stagger, `TK-P2-18` Pounce. **Open followups from TK-P2-13:** `TK-P2-20` late-join (network, S2), `TK-P2-21` B2 headless probe + wording (tools-devops), `TK-P2-22` nits bundle, `TK-P2-23` Change Log + TDD §9 barrier/poison doc (docs — do next for change-control), `TK-P2-19` CI kick-probe. **Agents:** architect + code-reviewer now opus (`a25043e`) — opus reviewer caught the TK-P2-13 S1 automation missed. **RESUME:** run the single human N-window checklist (Start happy-path N=3–8 + barrier B1/B2/B3 + WaitingRoom roster + Jump) → each card PASS → flip to done + close. Then next unblocked: `TK-P2-11` Jump-Kick (after Jump done), `TK-P2-14` Countdown (after Start done), `TK-P2-17` Kick Stagger, `TK-P2-18` Pounce. **Followup cards filed from TK-P2-13 review/qa:** `TK-P2-20` late-join-after-start (network, S2), `TK-P2-21` B2 headless probe + stale-wording fix (tools-devops), `TK-P2-22` nits bundle (grace-timer stop, disconnect_peer is_server guard, WaitingRoom literal-true + roster-broadcast guard, slot-capacity), `TK-P2-23` Change Log + TDD §9 barrier/poison doc (docs). Also open: `TK-P2-19` (CI kick-pipeline probe). **Agents:** architect + code-reviewer upgraded fable5→opus (`a25043e`) — opus reviewer caught the TK-P2-13 S1 that automation missed. **`TK-P2-09` CLOSED (2026-07-15): human 2-instance test PASS** — exactly one Tiger, every window agreed, no double/missing-Tiger. Flipped to `done`; unblocks `TK-P2-27` (all deps now done) and gives `TK-P2-07` a real call site. **Design ruling recorded for `TK-P2-06` (Safe Circle, Change Log [0.42]):** Tiger observed walking out of the circle during the TK-P2-09 pass is NOT a bug — it's TK-P2-06 scope. Ruling: Tiger confined inside radius 5.0m (invisible wall, value locked at [0.34]), Outer players move freely in/out, host-authoritative enforcement (host clamps/rejects position, no client-side escape). **`TK-P2-06` Safe Circle — CODE-COMPLETE, DUAL-REVIEWED, QA-AUTOMATION GREEN, BLOCKED ON REQUIRED HUMAN 2-INSTANCE PASS (NOT closed, 2026-07-16).** Tiger confined to 5.0m radius (owner-side clamp in `MovementComponent.gd` + host-side `correct_tiger_position` backstop in `GameManager.gd`), Outer players unrestricted. code-reviewer APPROVE-WITH-NITS; network-engineer first round NOT network-safe (host boundary check had zero tolerance → reliable RPC broadcast spam ~15-30/sec against an honest wall-pinned Tiger) → gameplay-engineer fixed (0.05m tolerance host-side only, RPC retargeted to owning peer via `rpc_id`, misleading comment corrected, 4 new tests). QA re-ran independently: GUT **276/276 (1428 asserts, 19 scripts)**, all 4 net probes PASS, DoD-vs-design-ruling checked test-by-test. Automation-only closure REJECTED (same S1-desync risk class as TK-P2-02/03/09/12/13 — host-authoritative position + a live correction RPC). Status `review-pending-human`; 5-case human 2-instance checklist written into the card's `notes` in `_backlog.json` (Tiger cannot exit; confinement agrees across windows; correction path is a total no-op / zero RPC spam against a wall-pinned honest Tiger — the exact bug just fixed; Outer walks freely in/out; role-swap transfers confinement correctly). **Non-blocking follow-up filed:** `TK-P2-28` (architect) — whether Phase 2 accepts owner-authoritative-position defense-in-depth for the Tiger boundary, or true host-authoritative Tiger position is warranted; does not block this card's human pass. **`TK-P2-06` CLOSED (2026-07-16): human 2-instance test PASS** — Tiger cannot walk through the 5.0m wall, Outer players move in/out freely, pushing the wall produces no jitter/rubber-band (live confirmation the tolerance fix held). **2 non-blocking follow-ups filed:** `TK-P2-29` (gameplay-engineer — wire Tag Sequence step 6's exit teleport to the real Safe Circle boundary instead of its old placeholder) and `TK-P2-30` (network-engineer — review whether the Start-Match 8s readiness grace-timer is tolerant enough for a slow-but-legit client, related to `TK-P2-20`). `TK-P2-28` (architect ruling on true host-authoritative Tiger containment) still open. **Next:** `TK-P2-07` RoundManager, `TK-P2-27` human feel/visual pass (speed profile + camera swap, now unblocked), or either follow-up. |
| Exit Gate goal | Kick + Tag + 7-step Tag Sequence + role swap on the ability scaffold; core loop playable |

## Phase 0 — CLOSED ✅ (Exit Gate PASS, PR #1 merged)
- clone→opens · 2 instances connect · `[NET] peer joined` · GUT green, no S0/S1. Cards `TK-P0-01`…`TK-P0-06`.

## Phase X — CLOSED ✅ (Exit Gate PASS, PR #2 merged `fd82306`)
- [x] Debug overlay (F3) + Perf overlay (F4) toggles, FPS/ping/state (manual GUI confirmed)
- [x] Settings change→save→reload persists across restart (headless round-trip + manual GUI confirmed)
- [x] ≥1 unit-test suite passing (GUT 88/88, 1135 asserts), Regression + NET SMOKE PASS, no S0/S1
- Cards `TK-PX-01`…`TK-PX-07`. Autoloads: GameLog, ConfigManager, NetworkManager, ErrorHandler, DebugOverlay, PerfOverlay.

## Phase 1 — CLOSED ✅ (Exit Gate PASS — human 2-window test + CI green)
- 6 build cards `TK-P1-01`…`TK-P1-06` + audit reopen fixes `TK-BUG-P1-01` (spawn authority/position via spawn_function + `_enter_tree`), `TK-BUG-P1-02` (host-quit → MainMenu + ESC-ESC Leave), `TK-P1-07` (CI gates on net smoke + spawn probe).
- [x] each peer spawns at its own slot (no stack/origin) + is authority of its own player
- [x] host quits → client returns to MainMenu; ESC-ESC Leave works (single ESC does NOT disconnect)
- [x] movement/camera sync across 2 windows — **human 2-window test PASSED**
- [x] Regression: GUT 119/119 (1206 asserts) + net_smoke + spawn probe (now CI-gated), no open S0/S1
- [x] Merge `develop` → `main` — **DONE**: Phase 1 close (`400e7f9` Exit Gate PASS) landed in `main` via **PR #4** (`386cf22`, 2026-07-05), which absorbed the pending PR #3. Phase 1 fully on `main`.
- Audit note [0.28]: S1-A "WASD dead (device:16)" was a FALSE ALARM (human-verified); the 116/116-green-but-broken lesson → per-card pure-function tests missed glue bugs; fixed by adding the 2-instance spawn probe to CI.

## Open cross-phase items (not blocking)
- `TK-P2-09` TigerSelector helper landed early (partial); full card OPEN pending `TK-P2-04` (Phase 2). Architect ack on `managers/` placement deferred to Phase 2.
- Phase-1 wiring picks up deferred infra: define InputMap actions (move/kick/tag) — unblocks Settings→Controls rebind (+ reconcile rebind string format "W"→"w"); audio Music/SFX bus layout still pending.
- Tech-debt: NetworkManager peer-teardown guard / MainMenu nits (task); PDF docs regen to 4.7.

## Next — Phase 2 (Core Loop), the "prove it's fun" milestone
**Gate before any Phase 2 code:** `architect` must approve the **Ability System** design (`TK-P2-16`: refactor Player → Movement / Camera / Ability components + base HumanAbility/TigerAbility; Kick/Tag re-slot as abilities, **Jump/Sprint stay as MovementComponent primitives** per design doc §2/§3 + architect ruling [0.33]/[0.34]). Then Phase 2 starts at that scaffold.
- Phase 2 card order (from `_backlog.json`, +8 new Cowork cards): `TK-P2-16` (Ability scaffold, first) → `TK-P2-01` Kick (ability, DONE) + `TK-P2-10` Jump (movement primitive) / `TK-P2-11` Jump-Kick → `TK-P2-12..15` match flow (Waiting Room → Start → Countdown → GameManager state machine) → Tag Sequence (`TK-P2-02..08`) → `TK-P2-09` first-Tiger (TigerSelector ready). `TK-P3-05` Tiger body-language (Crouch→Lean→Peek) proves the ability system.
- Ability catalog (Phase 3-5): Tiger{Crouch, Lean, Sprint, Pounce, Peek} · Human{Kick, Hide, Emote}.
