# --- Godot 4 ---
.godot/
/build/
/android/
.import/
*.translation
# export_presets.cfg is intentionally tracked (see TK-P3-02): it holds the
# shared "Windows Desktop" playtest-build preset, no local paths/secrets in it.

# --- Addons that are vendored separately (optional) ---
# addons/gut/    # keep if you commit GUT; ignore if installed per-machine

# --- OS / Editor ---
.DS_Store
Thumbs.db
*.swp

# --- Doc-tool / office temp (safe to ignore) ---
*.tmp
.~lock.*#
node_modules/

# --- Python tooling (tools/) ---
__pycache__/
*.pyc
